export enum SectionType {
  IMAGE = 'image',
  PARAGRAPH = 'paragraph',
  HEADING = 'heading',
  GLOBAL = 'global',
}

export enum AlignOption {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
}
